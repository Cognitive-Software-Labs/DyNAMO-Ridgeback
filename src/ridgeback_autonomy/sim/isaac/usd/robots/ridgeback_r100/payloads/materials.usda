#usda 1.0
(
    customLayerData = {
        string creator = "URDF USD Converter v0.1.3"
    }
    doc = """Generated from Composed Stage of root layer /tmp/tmp3jgwzs3u/r100-0001.usdc


Generated from Composed Stage of root layer /tmp/urdf_import_ridgeback_r100_8h97qxgu/usdex_ridgeback_r100/r100-0001.usdc


Generated from Composed Stage of root layer /tmp/urdf_import_ridgeback_r100_8h97qxgu/temp_ridgeback_r100/ridgeback_r100.usd


Generated from Composed Stage of root layer /home/deivid/dev/DyNAMO-Ridgeback/.claude/worktrees/isaac/src/ridgeback_autonomy/sim/isaac/usd/robots/ridgeback_r100.usd/ridgeback_r100/payloads/base.usd
"""
    kilogramsPerUnit = 1
    metersPerUnit = 1
    upAxis = "Z"
)

def Scope "Materials"
{
    def Material "clearpath_black"
    {
        color3f inputs:diffuseColor = (0.01960665, 0.01960665, 0.01960665)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/clearpath_black/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/clearpath_black/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/clearpath_black.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/clearpath_black.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/clearpath_black.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/clearpath_black.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }

    def Material "clearpath_yellow"
    {
        color3f inputs:diffuseColor = (0.6038274, 0.6038274, 0)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/clearpath_yellow/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/clearpath_yellow/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/clearpath_yellow.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/clearpath_yellow.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/clearpath_yellow.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/clearpath_yellow.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }

    def Material "clearpath_white"
    {
        color3f inputs:diffuseColor = (1, 1, 1)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/clearpath_white/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/clearpath_white/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/clearpath_white.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/clearpath_white.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/clearpath_white.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/clearpath_white.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }

    def Material "clearpath_red"
    {
        color3f inputs:diffuseColor = (1, 0, 0)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/clearpath_red/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/clearpath_red/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/clearpath_red.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/clearpath_red.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/clearpath_red.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/clearpath_red.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }

    def Material "clearpath_light_grey"
    {
        color3f inputs:diffuseColor = (0.6038274, 0.6038274, 0.6038274)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/clearpath_light_grey/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/clearpath_light_grey/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/clearpath_light_grey.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/clearpath_light_grey.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/clearpath_light_grey.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/clearpath_light_grey.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }

    def Material "clearpath_dark_grey"
    {
        color3f inputs:diffuseColor = (0.033104762, 0.033104762, 0.033104762)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/clearpath_dark_grey/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/clearpath_dark_grey/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/clearpath_dark_grey.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/clearpath_dark_grey.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/clearpath_dark_grey.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/clearpath_dark_grey.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }

    def Material "clearpath_aluminum"
    {
        color3f inputs:diffuseColor = (0.21404114, 0.21404114, 0.21404114)
        float inputs:metallic = 0
        float inputs:opacity = 1
        float inputs:roughness = 0.5
        token inputs:wrapMode = "repeat"
        token outputs:displacement (
            displayGroup = "Outputs"
        )
        prepend token outputs:displacement.connect = </Materials/clearpath_aluminum/PreviewSurface.outputs:displacement>
        token outputs:surface (
            displayGroup = "Outputs"
        )
        prepend token outputs:surface.connect = </Materials/clearpath_aluminum/PreviewSurface.outputs:surface>
        token outputs:volume (
            displayGroup = "Outputs"
        )

        def Shader "PreviewSurface" (
            apiSchemas = ["NodeDefAPI"]
        )
        {
            token info:id = "UsdPreviewSurface"
            prepend color3f inputs:diffuseColor.connect = </Materials/clearpath_aluminum.inputs:diffuseColor>
            prepend float inputs:metallic.connect = </Materials/clearpath_aluminum.inputs:metallic>
            prepend float inputs:opacity.connect = </Materials/clearpath_aluminum.inputs:opacity>
            prepend float inputs:roughness.connect = </Materials/clearpath_aluminum.inputs:roughness>
            token outputs:displacement
            token outputs:surface
        }
    }
}

